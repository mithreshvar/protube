{
  "name": "protube",
  "version": "1.0.0",
  "description": "protube app",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build",
    "start": "concurrently \"cd server && npm start\" \"cd client && npm start\" "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
